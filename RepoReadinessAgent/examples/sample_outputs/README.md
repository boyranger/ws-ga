# Sample Outputs

This directory can hold:
- prepared demo outputs
- example text reports
- example JSON reports
- future before/after follow-up examples

The purpose is to make the repo easier to demo and easier to understand without requiring a live run every time.
