# ws-ga

Simple workspace for `code.nalarahub.my.id`.

## Current frontend

- `index.html` — static landing page
- Tailwind loaded via CDN
- no Node.js / no `node_modules`

## Planned next steps

- Python backend
- Google OAuth login
- dashboard after auth
